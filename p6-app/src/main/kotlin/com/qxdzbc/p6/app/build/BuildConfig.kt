package com.qxdzbc.p6.app.build

/**
 * Contain build config
 * TODO see if I can wire this into the build system
 */
object BuildConfig {
    val currentFlavor = BuildVariant.DEBUG
//    val currentFlavor = BuildVariant.PRODUCTION
}
