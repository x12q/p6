package com.emeraldblast.p6.ui.window.state

import kotlin.test.BeforeTest

class WindowStateImpTest {
    lateinit var windowState: WindowStateImp
    @BeforeTest
    fun b() {
//        windowState = WindowStateImp.create(id = "abc",wbContMs = ms(WorkbookContainerImp()), wbStateContMs = ms(WorkbookStateContainerImp()))
    }
}
