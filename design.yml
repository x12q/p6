UI design:
  - cells make up sheets
  - sheets make up workbooks
  Cell:
    - cell can execute code:
        - python
        - primitive language (prim)
    Cell code:
      Restriction:
        not change the content of other cells: to do this, the user code must be wrapped inside an implicit function (python) to return error when changes are detected.
      Prohibition:

Tobe done:
  view:
    cell view
    simple code editor
